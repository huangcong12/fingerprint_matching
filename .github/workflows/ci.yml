name: CI

on: [push]

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: PHPUnit tests
        uses: php-actions/phpunit@v9
        with:
          php_extensions: FFI